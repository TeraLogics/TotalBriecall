define(['underscore', 'module'], function (_, module) {
	var brie = <%- JSON.stringify(brie) %>;

	// Extend page config with any provided local config
	_.extend(brie.page, module.config());

	return brie;
});